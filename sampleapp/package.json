{
  "name": "webapp.samples.todos",
  "version": "0.0.1",
  "dependencies": {
    "stack": ">=0.0.3",
    "creationix": ">=0.1.1",
    "topcube": ">=0.0.5",
    "@snyk/protect": "latest"
  },
  "private": true,
  "scripts": {
    "prepublish": "npm run snyk-protect",
    "snyk-protect": "snyk-protect"
  },
  "snyk": true
}
